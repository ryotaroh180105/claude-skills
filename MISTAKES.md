# MISTAKES.md — ミス・エラーのデータベース

Claude との会話で発見したミス・エラーを記録し、再発を防ぐためのデータベース。
スキーマ・ストレージ選定の根拠は `docs/mistakes-db-design.md` が正。

## 運用ルール

- ミスが発覚したら（ユーザー指摘・Claude 自身の気づきの両方）、**同じセッション内で**このファイルに追記してコミット・push する。後回しにしない。
- 登録手順: (1)記録ログをキーワード grep して重複チェック → 既出なら該当エントリの「再発」を更新しルール文を強化 (2)新規なら `git pull` して既存最大 M-NNN + 1 で採番 (3)ルール行とログエントリを**同一コミット**で追加。
- 「再発防止ルール」セクションは SessionStart フックが毎セッション自動でコンテキストに注入する。**1ルール1行・上限20行**。超えたら統合・CLAUDE.md 昇格・廃止のいずれかで削減する。
- スキーマ・上限は `scripts/validate_mistakes.py` が CI で機械検証する（自己申告に頼らない）。登録後はローカルでも同スクリプトを実行して OK を確認する。
- 毎月1日に Routine「MISTAKES.md 月次レビューループ」（LOOPS.md 参照）が棚卸しを実行し、昇格・統合・廃止を PR で提案する。マージ判断は Ryo。
- 一般化できるルールは CLAUDE.md へ昇格させ、ルール行を削除、ログのステータスを「昇格済み」にする。ログエントリは消さない（履歴・集計用）。
- 記録対象: 誤答・手順ミス・仕様誤解・確認漏れ・破壊操作など「二度と繰り返したくない」もの。タイポ等の些末なものは記録しない。
- このリポジトリ外の環境（claude.ai・ローカル Claude Code・他リポジトリ）への適用は
  `docs/global-rules.md` の移植キットに従う。push できない環境で発覚したミスは
  「M-登録依頼」（書式は同キット参照）として出力し、後からここに正式登録する。

<!-- rules:start -->
## 再発防止ルール（毎セッション自動読み込み）

- [M-001][誤答] 「このセッションからは Hermes/NotebookLM を使えない」と答えない。git push できれば hermes-relay 経由で実行可能。
- [M-002][手順ミス] 外部DB（Notion等）の調査はスキーマと保存形式の把握まで。件数集計・サンプル行取得を重ねない（設計判断に必須の場合のみ、必要性を添えて最小限）。
<!-- rules:end -->

## 記録ログ

エントリ書式（全フィールド必須）:

```markdown
### M-NNN: <タイトル>
- 登録日: YYYY-MM-DD / 環境: <claude.ai|code-local|code-remote> / カテゴリ: <誤答|手順ミス|仕様誤解|確認漏れ|破壊操作|その他>
- 事象: <何が起きたか>
- 根本原因: <なぜ起きたか>
- 再発防止: <ルール行と同文または詳細版>
- 再発: なし | YYYY-MM-DD (計N回)
- ステータス: active | 昇格済み(CLAUDE.md) | 廃止(<理由>)
```

### M-001: リモートセッションで Hermes/NotebookLM を「使えない」と誤答

- 登録日: 2026-07-05 / 環境: code-remote / カテゴリ: 誤答
- 事象: プラグイン未導入のリモートセッションで、調査依頼に対し「Hermes/NotebookLM は使えない」と回答した。
- 根本原因: `/plugin` でのスキル読み込み可否と、hermes-relay ブランチ経由の実行可否を混同した。
- 再発防止: 調査依頼が来たら、まず hermes-relay リレー手順（CLAUDE.md 参照）を確認する。
- 再発: なし
- ステータス: active（このリポジトリの CLAUDE.md にも記載あり。ルール行は他環境への注入用に維持）

### M-002: Notion DB 調査でスキーマ把握を超えて中身を読み込みすぎた

- 登録日: 2026-07-10 / 環境: code-local / カテゴリ: 手順ミス
- 事象: 検索エージェントの設計依頼で、Notion の4DBに対しスキーマ取得に加えて件数集計・GROUP BY・サンプル行取得のクエリを複数回実行し、「切りがない。どのDBにどんな感じで情報が保存されてるかだけ把握すれば十分」と指摘された。
- 根本原因: 設計の裏付けを厚くしようとして調査の停止条件を決めずにクエリを重ねた。スキーマ＋保存形式が分かれば設計判断には足りることを見誤った。
- 再発防止: 外部DB調査はスキーマと保存形式の把握（各DB1回のfetch程度）まで。追加クエリは設計判断がその事実に依存する場合のみ、必要性を添えて最小限にする。
- 再発: なし
- ステータス: active

## レビュー履歴

月次レビューループ（LOOPS.md 参照）が毎月2行以内で追記する。

- 2026-07: 初期設計。ルール1件/エントリ1件。次回 2026-08-01 が初回自動レビュー。
