web_search（Web検索）を使って調べてください。x_search は使わないでください。回答末尾に使用ツール名を列挙してください。
取得成功したソースの内容だけ書いてください。失敗時は創作せず『取得失敗』とだけ書いてください。

テーマ: Claude Code の `claude mcp add` で追加できる、OpenAI（ChatGPT, DALL-E / gpt-image-1）の画像生成を呼び出せるMCPサーバー（2025-2026年時点）。

調べてほしいこと:
1. OpenAI公式または信頼できるOSSで、Images API（gpt-image-1 / DALL-E 3）をMCPツールとして公開しているサーバーの有無。GitHubリポジトリ名・パッケージ名（npm/pip）・スター数目安
2. インストール方法（`npx` / `pip install` / `claude mcp add` コマンド例）
3. 必要な認証情報（OpenAI APIキー）と取得手順・料金（1枚あたりの目安コスト）
4. 実際にMCPツール経由で画像ファイルを生成し保存できるか（Claude Codeのようなエージェントから呼び出した実例・レビュー）

出力セクション:
1. 該当MCPサーバー一覧（名称/GitHub URL/インストールコマンド、出典URL）
2. 必要な認証・料金（出典URL）
3. 実利用レビュー・実例（出典URL）
4. 未確認・断定できない点
5. 使用ツール名一覧
